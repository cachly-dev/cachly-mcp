# Local development overrides — not used in production
# This file is auto-loaded by `docker compose up`
services:
  backend:
    ports:
      - "8000:8000"   # expose backend directly for local dev/testing
    environment:
      DEBUG: "true"

  caddy:
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"   # also expose dev HTTP port
